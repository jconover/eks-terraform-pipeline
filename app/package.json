{
  "name": "sample-app",
  "version": "1.0.0",
  "description": "Sample application for EKS deployment",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "prom-client": "^14.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}